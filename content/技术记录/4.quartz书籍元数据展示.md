
## 书籍元数据展示
obsidian中的md文档元数据展示在quartz中

### 1. 确保 Frontmatter 格式正确
如:
```yaml
---
title: 书名
author: 作者
rating: 5
status: 已读
---
```

### 2. 配置 Quartz 显示 Frontmatter
- 在`quartz/components/`中,创建一个新文件`BookInfo.tsx`
```tsx
import { QuartzComponent, QuartzComponentConstructor, QuartzComponentProps } from "./types"

export default (() => {
  const BookInfo: QuartzComponent = ({ fileData }: QuartzComponentProps) => {
    // 获取 frontmatter 数据
    const frontmatter = fileData.frontmatter
    
    // 如果没有 frontmatter，不显示任何内容
    if (!frontmatter) return null
    
    // 检查是否有书籍相关的字段
    const hasBookInfo = frontmatter.author || frontmatter.status || frontmatter.rating
    if (!hasBookInfo) return null
    
    return (
      <div class="book-info">
        <h3>📚 书籍信息</h3>
        {frontmatter.author && (
          <p><strong>作者：</strong>{frontmatter.author}</p>
        )}
        {frontmatter.status && (
          <p><strong>状态：</strong>{frontmatter.status}</p>
        )}
        {frontmatter.rating && (
          <p><strong>评分：</strong>{frontmatter.rating} / 5</p>
        )}
      </div>
    )
  }
  
  return BookInfo
}) satisfies QuartzComponentConstructor
```
- 导出组件
在 `quartz/components/index.ts`中
```ts
import ArticleTitle from "./ArticleTitle"
import Content from "./Content"
import TagList from "./TagList"
// ... 其他导入

import BookInfo from "./BookInfo"  // 添加这一行

export default {
  ArticleTitle,
  Content,
  TagList,
  // ... 其他组件
  BookInfo,  // 添加这一行
}
```
- 注册组件
在`quartz.layout.ts`中,添加新建的组件
```ts
export const defaultContentPageLayout: PageLayout = {
  beforeBody: [
    Component.Breadcrumbs(),
    Component.ArticleTitle(),
    Component.BookInfo(),  // 添加这一行
    Component.ContentMeta(),
    Component.TagList(),
  ],
  // ...
}
```
- 添加样式
 在`quartz/styles/custom.scss` 中添加
```scss
.book-info {
  background-color: #f0f8ff;
  border-left: 4px solid #4a90e2;
  padding: 15px;
  margin: 20px 0;
  border-radius: 5px;
  
  h3 {
    margin-top: 0;
    color: #2c5282;
  }
  
  p {
    margin: 8px 0;
  }
}
```