
实现方案
- mixa 简易但个性化程度低,高需求需付费
- quartz 难度适中,可个性化修改页面, 免费

quartz方案步骤
1. 安装依赖
> [node.js](https://nodejs.org/zh-cn/download)
> [git](https://git-scm.com/install/)
> 进入系统终端检查是否安装成功
```
# 检查Node版本
node -v  # 输出：v20.x.x
 
# 检查Git安装是否成功
git -v   # 输出：git version x.xx.x
```

2. 安装quartz
```
# 克隆Quartz远程仓库
git clone https://github.com/jackyzha0/quartz.git
 
# 进入quartz目录
cd quartz

# 安装相关依赖
npm i

# 构建quartz
npx quartz build --serve
 
```

3. quartz 配置
打开`quartz`根目录文件夹，找到`quartz.config.ts`即可开始编辑。
- `pageTitle`：站点名称。
- `locale`：指定站点语言。影响Quartz界面的语言，例如中文就可以设置为`zh-CN`。
- `baseUrl`：指定站点网址。

4. github配置
- 登陆自己的github账号新建一个仓库
	>**不要使用”README”、.gitignore、license** 。Quartz 有自己的 git 设定方式勾选了会导致功能异常
- 获得仓库链接:https://github.com/xxxxxx
- 确认终端在quartz目录下分布执行命令
```
# 列出所有远程仓库的详细信息
git remote -v
 
# 应该会见到以下输出：
origin  https://github.com/jackyzha0/quartz.git (fetch)
origin  https://github.com/jackyzha0/quartz.git (push)
upstream        https://github.com/jackyzha0/quartz.git (fetch)
upstream        https://github.com/jackyzha0/quartz.git (push)
```

```
# 将远程仓库链接设置为自己的远程仓库链接
git remote set-url origin REMOTE-URL 
```

```
# 将内容推送到远程仓库
npx quartz sync
```

5. cloudflare pages
这里也可以只把页面部署到GitHub pages,或不通过github 直接部署到cloudflare pages,选择现在GitHub构建仓库再推送到cloudflare pages 是因为github可以对代码进行追踪管理,cloudflare pages的页面优化做得更好
- 登陆cloudflare账号
- 左侧菜单栏选择computer(workers)-->workers 和pages
- 创建pages并连结到git
- 授权验证后选择对应仓库
- 保存并部署
>[!warning]
> 分支选择main
   注意:这里选main而不是v4,v4是用作升级参考的,main才是你自己修改和管理的分支
   构建npx quartz build
> 输出目录public
- 如果自己有域名可以更改为自己的域名

6. 页面发布

[git](https://git-scm.com/install/)


[[https://git-scm.com/install/]]